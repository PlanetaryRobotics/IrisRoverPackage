/*
 * Hardware Abstraction Layer for the Motor Driver (and related hardware).
 *
 * TODO: Implement (pull over the EVM test code).
 *
 * Author: Connor W. Colombo, CMU
 */

#include <Driver_HAL.h>
#include <stdbool.h>

void setPWM(uint16_t duty){
    // TODO (pull over the EVM test code)
}

int16_t getCurrentA(void){
    // TODO (pull over the EVM test code)
    return 0;
}
int16_t getCurrentB(void){
    // TODO (pull over the EVM test code)
    return 0;
}
int16_t getCurrentC(void){
    // TODO (pull over the EVM test code)
    return 0;
}
int16_t getCurrentNorm(void){
    // TODO (pull over the EVM test code)
    return 0;
}

uint8_t getHall1State(void){
    // TODO (pull over the EVM test code)
    return 0;
}
uint8_t getHall2State(void){
    // TODO (pull over the EVM test code)
    return 0;
}
uint8_t getHall3State(void){
    // TODO (pull over the EVM test code)
    return 0;
}

int32_t getPosition(void){
    // TODO (pull over the EVM test code)
    return 0;
}

uint32_t getTotalTimeDelta(void){
    // TODO (pull over the EVM test code)
    return 0;
}
