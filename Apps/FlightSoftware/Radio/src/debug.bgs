# universally accessible debug flag
export const DEBUG = 1

# Warn on boot if we're in debug:
event system_boot(major,minor,patch,build,bootloader,tcpip,hw)
    if DEBUG = 1 then
        call hercules_direct_message_ascii(34, "WARNING: Radio DEBUG mode active.\n")
    end if
end
