/*
 * Code for interfacing with UART protocol hardware module.
 *
 * UART communication uses eUSCI_A0 (Hercules) and eUSCI_A1 (Lander).
 */
#include <msp430.h>
#include "include/uart.h"

/* =============================== main code ================================ */

/* init function */
void uart_init() {
    // cleared since I need to rewrite this code
}
